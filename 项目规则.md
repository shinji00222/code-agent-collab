# 项目规则（给人看的总览）

这份文档是项目所有规则的入口，适合人直接阅读。给 AI 看的详细协作规则在 `AGENTS.md`。

## 一、项目是干什么的

一个本地优先的命令行工具：给一个任务，6 个角色 Agent 顺序协作（拆任务 → 查知识 → 出计划 → 写代码草稿 → 检查 → 复盘），AI 产出先进 `dev-vault` 待确认区，不直接改正式源码、不写主知识库。

## 二、规则清单

| 规则 | 文档 | 一句话说明 |
| --- | --- | --- |
| 协作方式 | `AGENTS.md` | Codex 与项目协作：直接推进 + 关键节点教学 |
| 版本管理 | `版本管理.md` | 版本分类、版本号规则、发布流程 |
| 技能沉淀 | `技能.md` | 可复用经验怎么写、怎么更新 |
| **问题台账** | 桌面《多Agent代码协作助手-当前问题整理.md》+ `AGENTS.md` | **发现的问题和已修复的问题都要及时回填台账** |
| 知识库边界 | `知识地图.md` | 项目自有知识库读写、外部知识库默认不接入、知识流向 |
| 维护方法 | `维护指南.md` | 目录结构、常见改动、测试、常见问题 |

## 三、红线（必须遵守）

- 不删除、覆盖用户文件，不扩大任务范围。
- 不把 API Key、密码、令牌、Cookie、本机路径写进仓库、文档或日志。
- 知识库默认全隔离：读和写都在项目自有知识库 `dev-vault/project-vault`，接入外部知识库必须显式配置。
- 上传 GitHub、发布 Release 前：测试通过、仓库干净、安全扫描（敏感信息）、带版本说明。
- **发现的问题必须写进问题台账**；修复后立刻回填状态、提交号和验证方式，不能只留在聊天记录或 `变更记录.md` 里。
- 文档文件名用中文（代码与测试文件名保持英文，Python 模块名限制）。

## 四、问题台账怎么用

台账文件：`C:\Users\<你的用户名>\Desktop\多Agent代码协作助手-当前问题整理.md`（目前在本机桌面，不在项目目录内）。

- 每条问题写清：编号（`N` 序号）、优先级（`P0`–`P3`）、**大白话解释 + 专业术语解释**、证据（文件:行号 / 命令 / 实测输出）、建议方案（改哪里、风险、怎么验证、工作量）。
- 修好的问题在原条目里标"已修复"，并写：修复版本号、本地提交号、验证方式、遗留限制。
- 台账里保留一份"已修复清单"，避免下次重复排查同一个问题。
- **没有验证证据就不能标"已修复"**，也不写"应该修好了"。
- 每次任务收尾前自检：本轮新发现的问题和修复的问题，台账里是否都反映了。

## 五、仓库干净要求

- `.gitignore` 已忽略：本地配置、运行日志、候选记录、代码草稿。
- `git status` 应保持干净；新功能完成后及时提交。
- 发布公开快照不含本机路径、API Key 和测试产物。
