# 项目自有知识库（project-vault）

这个目录是**本项目自己的知识库**。默认情况下，程序的知识**全部读写都发生在项目内**，
不会读、也不会写你电脑上那个真实的知识库。

## 为什么要有它

这个工具原先是「读你的主知识库（只读）+ 把确认的笔记写进主知识库」。
两个方向都有问题：

- **写**：`confirm` 直接写主知识库，目标文件名沿用候选文件名且没有存在性检查，
  同一条候选确认两次就会**静默覆盖**既有文件（包括你手工补充的内容）。
- **读**：程序会去检索你电脑上的一整个知识库目录。只要配置写错或缺失，
  就可能读到不该读的地方，属于权限过宽。

所以现在改成：**读写都收进这个项目目录**。要接真实知识库，必须显式配置。

## 行为说明

| 动作 | 默认目标 | 怎么改成真实知识库 |
| --- | --- | --- |
| `review`（AI 审查候选） | 只改 `dev-vault/pending` 的状态行 | 不涉及 |
| 知识检索（KnowledgeAgent） | **本目录** | 配置 `mainVaultPath` |
| `confirm`（人工确认入库） | **本目录** | 配置 `mainVaultWritePath` |

配置方式（二选一）：

```jsonc
// .agent-workbench/config.json
{
  "mainVaultPath": "C:/你的/真实/知识库",          // 读取来源
  "mainVaultWritePath": "C:/你的/真实/知识库"      // 写入目标
}
```

或者用环境变量（适合一次性操作，优先级更高）：

```powershell
$env:AGENT_WORKBENCH_MAIN_VAULT       = "C:\你的\真实\知识库"   # 读取
$env:AGENT_WORKBENCH_MAIN_VAULT_WRITE = "C:\你的\真实\知识库"   # 写入
```

**两个都不配 = 完全隔离**。这是刻意的安全默认值，不是配置漏了。

## 目录约定

- 本目录里真正写入的 `*.md` 笔记**不进版本库**（见根目录 `.gitignore`）。
- 只有这个 `README.md` 和各级 `.gitkeep` 占位文件会进版本库。
- 目录结构刻意与主知识库的常用入库位置保持一致（含 `04-知识/02-Codex知识/04-复盘`），
  这样主控 AI 建议的相对路径在这里也能解析成功。
- 想清空它，直接删本目录下的笔记即可，不会影响任何真实知识库。

## 想让它真正有用

默认是空的，所以检索一开始什么都搜不到。两种做法：

1. 把你想让项目参考的笔记**复制**（不是移动）进来；
2. 或者显式把 `mainVaultPath` 指向你的真实知识库 —— 但那样就重新读外部目录了，
   请确认这是你想要的。

## 当前收录的副本（2026-09-18 复制）

下面 8 份是从外部知识库**复制**进来的，用于让 KnowledgeAgent 检索时有真实依据。
路径结构与来源保持一致，方便对照和重新复制。

| 本目录内路径 | 来源 | 为什么收录 |
| --- | --- | --- |
| `00-系统/全局工作台.md` | 外部知识库同名文件 | 全局协作规则，项目文档多处引用它 |
| `00-系统/多Agent共用工作台规则.md` | 外部知识库同名文件 | 多 Agent 共用工作台的边界与交接规则，与本项目主题直接相关 |
| `04-知识/02-Codex知识/01-工作方法/我的项目工作规范.md` | 外部知识库同名文件 | 项目存放、验证、隐私的硬规则 |
| `04-知识/02-Codex知识/01-工作方法/用户偏好与协作方式.md` | 外部知识库同名文件 | 协作偏好，直接决定 Agent 该怎么汇报和推进 |
| `04-知识/02-Codex知识/02-参考/参考与边界.md` | 外部知识库同名文件 | 知识库该存什么、不该存什么 |
| `04-知识/02-Codex知识/03-架构决策/ADR-0001-知识库分层.md` | 外部知识库同名文件 | 知识库分层决策，本项目知识流程的设计依据 |
| `04-知识/02-Codex知识/05-AI协作/AI协作协议.md` | 外部知识库同名文件 | 多 Agent 分工协议 |
| `04-知识/02-Codex知识/05-AI协作/交接模板.md` | 外部知识库同名文件 | 任务交接格式模板 |

### 刻意没有收录的

| 来源 | 原因 |
| --- | --- |
| `00-系统/复利踩坑日志.md` | 有 **231 KB**，超过检索器单文件上限（`DEFAULT_MAX_FILE_BYTES = 200_000`），复制进来也会被**整个跳过**，等于没用 |
| `00-系统/AGENTS.md` | 与本项目自己的 `AGENTS.md` 同名，容易混淆两套规则 |
| `00-系统/代理与Codex使用说明.md` | 是本机代理配置，不是项目知识 |

### 副本会过期（重要）

这些是**副本**，不会随原文自动更新。

- 改了原文 → 本目录里的副本还是旧的，检索到的是旧结论。
- 想刷新：把上表里的源文件重新复制一遍覆盖即可。
- 建议只收录**稳定的**东西（规则、协议、决策、方法论），
  **不要**复制天天更新的日志类笔记 —— 过期的副本比没有副本更误导。

### 隐私边界

这些副本含本机路径与个人偏好，因此**必须保持被 `.gitignore` 忽略**
（规则：`dev-vault/project-vault/**/*.md`）。它们只存在于本机，不会进版本库、不会被推送。
如果以后有人改动那条忽略规则，就相当于把这些个人笔记送进公开仓库 —— 改之前务必先确认。
