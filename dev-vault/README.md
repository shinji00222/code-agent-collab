# dev-vault

这是本项目的开发测试知识库。

规则：

- 允许开发阶段读写；
- 自动生成内容先进入这里；
- 不直接同步到主知识库；
- 进入主知识库前必须经过用户确认。

目录用途：

- `pending/`：候选复利记录，等待用户确认保留、废弃或同步。
- `projects/`：AI 生成的代码草稿（如 CoderAgent 草稿），未经确认不得合并到正式源码。
- `rules/`：项目实验规则，确认后可选同步到主知识库。

注意：`pending/` 和 `projects/` 里的内容属于本地工作产物，已被 `.gitignore` 忽略，不会进入 Git。
