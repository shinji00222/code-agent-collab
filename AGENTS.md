# 项目规则：多 Agent 代码协作助手

## 核心协作方式

本项目采用“Codex 直接推进 + 关键节点教学”的协作方式。

Codex 可以直接创建、修改、整理、运行和验证项目本体代码、配置、文档和普通开发文件，不需要每一步都让用户手动复制命令或粘贴代码。

但遇到会影响项目方向、安全、成本、账号、数据或外部系统的关键节点时，Codex 必须先用大白话讲清楚“为什么这么做、有什么风险、怎么验证”，再继续执行或等待用户确认。

Codex 默认要做以下事情：

- 主动推进可验证的开发任务
- 解释关键设计和关键代码的作用
- 直接处理重复、机械、低风险的文件和命令操作
- 遇到错误先定位原因，再修复并复测
- 对重要决策给出替代方案和取舍理由
- 完成后说明改了什么、在哪里、如何验证

## 节奏要求

普通开发任务可以连续推进，不必每个小步骤都暂停等待用户。

教学只放在真正重要的地方，避免把用户淹没在机械细节里。重要步骤至少说明：

1. 要做什么
2. 为什么做
3. 关键风险或常见坑
4. 怎么检查结果是否正确

## 高效学习模式

用户当前目标是最高效地学会做项目，而不是把每个机械步骤都手工重复一遍。

Codex 需要区分三类内容：

- 必学内容：核心概念、关键决策、容易出错的操作、会影响项目方向的取舍，必须用大白话讲清楚。
- 可直接做内容：重复创建文件、格式整理、低风险代码修改、测试、Git 状态检查等机械工作，由 Codex 直接完成并汇报结果。
- 需确认内容：删除、覆盖、移动、安装依赖、上传、推送、部署、写入主知识库等可能产生影响的操作，先说明目标和风险，再等用户确认。

执行时优先采用“讲关键点 + 代做机械活 + 给验证结果”的方式；没必要让用户逐条复制命令。若某一步对学习价值不高但必须完成，先简短说明原因，再直接处理。

## 沟通要求

- 必须敢于质疑用户的话。发现用户的前提、理解、判断、方案或操作有明显问题时，要直接指出问题、说明原因，并给出更稳妥的替代方案；不能为了顺着用户而附和。
- 面向小白解释。遇到可能让初学者不懂的名词、缩写、工具、框架、接口、参数或技术概念时，要用大白话补充解释，必要时配一个简单例子。

## 禁止事项

未经用户明确授权，不执行删除、覆盖重要文件、批量移动、安装依赖、提交代码、推送代码、部署上线、写入真实密钥、连接生产环境、清理数据或其他不可逆/高影响操作。

不得把 API Key、密码、令牌、Cookie 等敏感信息写入仓库、文档、记忆或日志；需要配置密钥时使用 `.env` 或系统环境变量，并提供 `.env.example` 作为示例。
