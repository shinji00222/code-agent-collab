# 知识地图（项目知识怎么串联）

这份文档说明项目的知识存在哪、谁生成、怎么被使用，让人一眼看懂知识的流向。

## 一、知识分层与位置

| 层 | 位置 | 内容 | 谁写 |
| --- | --- | --- | --- |
| 规则 | `AGENTS.md`、`项目规则.md`、`版本管理.md` | 协作、版本、发布规则 | 人 |
| 技能 | `技能.md` | 可复用经验、标准流程 | 人 + AI 沉淀 |
| 产品文档 | `product-docs/` | 需求、企划、格式、边界 | 人 |
| AI 产出 | `dev-vault/pending`、`dev-vault/projects`、`dev-vault/rules` | 候选经验、代码草稿、实验规则 | AI |
| **项目自有知识库** | `dev-vault/project-vault` | 知识的**读写默认位置**（检索来源 + confirm 落点），与外部知识库隔离 | 人（confirm 触发） |
| 运行记录 | `logs/context-packs`、`logs/workflows` | 上下文包、工作流日志 | AI |
| 外部知识库（默认不接入） | 例如 AI 工作台知识库 | 长期知识、全局规则；**只有显式配置 `mainVaultPath` 才会被读取** | 人 |

## 二、知识流向（一次任务）

```text
你输入任务
  ↓
上下文包 logs/context-packs/<任务ID>.md：装下任务目标、项目文档、知识库边界
  ↓
KnowledgeAgent 从知识库只读检索 → 生成 <任务ID>-knowledge.md（相关经验摘录）
  （默认检索项目自有知识库 dev-vault/project-vault，不读外部知识库）
  ↓
PlannerAgent 结合检索结果出计划 → CoderAgent 生成草稿（dev-vault/projects）
  ↓
ReflectorAgent 生成候选复利记录（dev-vault/pending）
  ↓
AI 审查（review）：本地脱敏扫描 + AI 判断（只标记状态，不写知识库）
  ├── 安全 → 标记"待人工确认"，并记录 AI 建议的写入位置
  └── 敏感或 AI 不建议 → 标记"待人工处理"
  ↓
你 confirm 才写入知识库（仍会拦截敏感信息）；
默认写进项目自有知识库 dev-vault/project-vault，外部知识库零写入；
只有显式配置 mainVaultWritePath 才会写向外部知识库；discard 则废弃
```

## 三、外部知识库相关位置（默认不接入）

以下是外部知识库（AI 工作台知识库）里与本项目相关的笔记，**默认不会被读取**；
只有显式把 `mainVaultPath` 指向那个知识库时才会参与检索：

- `00-系统/全局工作台.md`：全局工作台规则。
- `00-系统/复利踩坑日志.md`：全局踩坑日志。
- `04-知识/02-Codex知识/05-AI协作/`：AI 协作协议、交接模板。
- `04-知识/02-Codex知识/01-工作方法/`：工作方法笔记。

默认情况下程序对**外部知识库既不读也不写**：读取来源和写入目标都是项目自有知识库
`dev-vault/project-vault`。要整理外部知识库本身时，先明确范围再人工确认。

## 四、检索机制

- 关键词提取：从任务目标提取中英文关键词（过滤口语词）。
- 检索范围：`config.json` 的 `mainVaultPath`（或环境变量 `AGENT_WORKBENCH_MAIN_VAULT`）；**默认是项目自有知识库 `dev-vault/project-vault`**。
- 写入范围：`config.json` 的 `mainVaultWritePath`（或环境变量 `AGENT_WORKBENCH_MAIN_VAULT_WRITE`）；默认与检索范围相同。
- 排除目录：隐藏目录、`99-附件`、`work`、`01-项目` 等（见 `src/code_agent_collab/knowledge.py`）。
- 结果：按命中数排序，生成知识补充文件给后续 Agent。
