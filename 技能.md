# 项目 Skills：多Agent代码协作助手

记录本项目已验证、可复用的开发技能。任务中积累的新经验先在这里沉淀，避免重复踩坑。

## 1. 多 Agent 顺序工作流

- 做什么：任务按 Coordinator → Knowledge → Planner → Coder → Reviewer → Validator → Reflector 顺序执行，每个 Agent 产出 `AgentResult` 并记录日志。
- 为什么：职责单一、顺序可控，便于审计每一步；Coder 产出后先经 Reviewer 评审，避免问题草稿继续往下流。
- 怎么做：Agent 之间只共享上下文包（context-pack），不共享聊天记录；每个 Agent 用不同 system prompt 区分职责；Agent 列表统一由 `build_workflow_agents()` 构建（内部走 `agents/registry.py` 的 `create_agent` 工厂）。
- 怎么验证：`python -m unittest discover -s tests`，工作流测试断言 Agent 数量与顺序。
- 常见坑：新增 Agent 后忘记同步 `agents/__init__.py`、`workflow.py` 和 `tests/test_workflow.py` 的断言数量；需要按复杂度动态派活时用 `run-adaptive`（见第 12 节），不要硬改默认流水线。

## 2. Provider 抽象（mock / DeepSeek / OpenAI）

- 做什么：所有 AI 调用走统一 `AIProvider` 接口，默认 mock。
- 为什么：不把模型写死在业务里，DeepSeek/OpenAI/本地模型可切换。
- 怎么做：集中预设表 `PROVIDER_PRESETS` 定义 `base_url`/`model`/`api_key_env`；通过 `AGENT_WORKBENCH_PROVIDER` 选择，通过 `AGENT_WORKBENCH_MODEL`、`AGENT_WORKBENCH_BASE_URL`、`AGENT_WORKBENCH_API_KEY_ENV` 覆盖默认值；API Key 只从环境变量读取，绝不写入项目文件、日志或文档。
- 怎么验证：`test_providers.py` 覆盖默认 mock、缺少 Key 时安全提示、未知 Provider 拒绝。
- 常见坑：把 Key 写进 `config.json` 或 CHANGELOG；mock 结果与真实模型差异大，接真实模型前先单独验证；给用户配置 DeepSeek/OpenAI 时优先用 `scripts/setup-deepseek.ps1`，用数字菜单处理厂商选择、配置、停用、查状态和启动 Web UI，只写 Windows 用户环境变量，不让 Key 进入聊天、仓库、日志。

## 2.1 新增一个 Provider 的标准流程

1. 在 `PROVIDER_PRESETS` 增加预设：`model`/`base_url`/`api_key_env`。
2. 在 `SUPPORTED_PROVIDERS` 中自动出现，`provider` 命令即可列出。
3. 需要自定义请求格式时，在 `providers.py` 扩展 `OpenAICompatibleProvider` 或新增 Provider 类，并接入 `create_provider`。
4. 在 `tests/test_providers.py` 增加预设断言和环境变量覆盖断言。
5. 更新 README 配置说明和 CHANGELOG。

## 3. 上下文包（context-pack）

- 做什么：每轮任务生成固定模板的上下文包（目标/必须遵守/相关文件/已知风险/允许范围/当前状态），写入 `logs/context-packs`。
- 为什么：聊天记录不能当数据库，长任务需要可重复读取的项目事实。
- 怎么做：固定目录 + 关键词匹配 + 文件摘要，暂不引入向量数据库。
- 怎么验证：`test_context_pack.py`。

## 4. 知识库隔离与候选沉淀

- 做什么：主知识库默认只读；AI 产出先进 `dev-vault`（`pending`/`projects`/`rules`），经人工确认后才能进主知识库。
- 为什么：防止 AI 自动污染长期知识库。
- 怎么做：写主知识库属于 `L3_CONFIRM_REQUIRED`；复盘候选先进 `dev-vault/pending`。
- 怎么验证：`test_demo.py` / `test_reflection.py` 断言只写 dev-vault。

## 5. 版本管理

- 做什么：区分开发版/实验版/稳定版/发布版；语义化版本；发布版必须 CHANGELOG + tag + 干净快照分支。
- 为什么：开发中间态和正式发布混在一起会导致回滚和排查困难。
- 怎么做：新功能模块升次版本；发布前测试通过、Git 状态干净、更新 CHANGELOG、打 tag；公开仓库用干净快照分支。
- 常见坑：只上传代码不写版本说明；忘记同步 CHANGELOG/README；快照分支命名与用途不符（如 `agents/model-inquiry` 实际是 v0.3.0 发布快照）。

## 6. 新增一个 Agent 角色的标准流程

1. 在 `src/code_agent_collab/agents/` 新建 `<name>.py`，继承 `BaseAgent`，定义 `role` 和 `permission`。
2. 在 `agents/__init__.py` 导出并加入 `__all__`。
3. 在 `agents/registry.py` 的 `create_agent` 工厂登记角色（需要 provider 的 Agent 走 provider 参数）。
4. 在 `workflow.py` 的 `build_workflow_agents()` 加入角色名和位置。
5. 更新 `tests/test_workflow.py` 的 Agent 数量断言和新增角色的断言。
6. 更新 README 角色列表、变更记录，必要时更新 版本管理.md。
7. 跑全部测试，确认通过后再提交。

## 7. 主知识库只读检索

- 做什么：`KnowledgeAgent` 从任务目标提取关键词，只读检索主知识库中的相关 Markdown，生成 `logs/context-packs/<任务ID>-knowledge.md` 知识补充文件。
- 为什么：让长期知识参与任务上下文，同时保证主知识库不被自动污染。
- 怎么做：`extract_keywords`（中英文关键词 + 停用字过滤）→ `search_knowledge`（限制深度、文件数、文件大小）→ `render_knowledge_note` 输出补充文件。
- 怎么验证：`tests/test_knowledge.py` 覆盖关键词提取、命中、排除目录、只读性和 Agent 集成。
- 常见坑：中文没有内置分词，长句滑窗会切出噪音词，需用停用字过滤；主知识库可能很大，必须限制深度和文件数，并排除 `99-附件`、`work`、`01-项目` 等目录；检索只读，绝不写入主知识库。
- 主知识库路径可通过 `AGENT_WORKBENCH_MAIN_VAULT` 环境变量覆盖，优先级高于 `.agent-workbench/config.json`。

## 8. 知识入库审查

- 做什么：`review` 命令审查 `dev-vault/pending` 的候选记录，AI 审查通过**只标记"待人工确认"并记录 AI 建议的写入位置，不自动写入主知识库**；命中敏感信息的标记"待人工处理"。主知识库写入只有人工确认（`confirm`）一条通道。
- 为什么：AI 只能提议，最后闸门必须是人；敏感信息必须人工处理。
- 怎么做：本地正则扫描（`scan_sensitive`，不依赖 AI）→ AI 判断（`REVIEW_VERDICT`/`REVIEW_REASON`/`REVIEW_TARGET`）→ 通过后把 `- AI建议写入位置：<相对路径>` 记入候选记录并标记"待人工确认"；`confirm` 读取该建议作为写入目标（目标必须位于主知识库内现有目录，否则回退默认复盘目录），写入前清洗本机路径。
- 怎么验证：`tests/test_review.py` 覆盖敏感拦截、mock 审查不自动入库、人工确认（含使用 AI 建议位置）、废弃、路径安全。
- 常见坑：mock Provider 会回显提示词，必须只采信明确的 `approve`/`risky` 值，否则误判；候选模板的元数据路径（含 `- AI建议写入位置：` 行）不算敏感信息，扫描前要先移除元数据行；AI 输出无法解析时一律转人工处理；`confirm` 仍会做敏感扫描，人工确认不能绕过。

## 9. Web 终端界面

- 做什么：`python -m code_agent_collab.webui` 启动本机网页终端，浏览器里输入命令执行项目功能。
- 为什么：命令行对初学者不直观，网页终端更接近日常使用习惯；可视化进度树能直接看到 Agent 之间怎么流转、当前卡在审批还是执行。
- 怎么做：标准库 `http.server` 提供页面、`/api/command` 命令接口、`/api/discuss` 聊天接口、`/api/progress` 只读进度接口、`/api/pause` 软暂停接口和 `/api/force-stop` 强制停止接口；普通输入先进入聊天，用户问问题时先回答具体问题，不只追问需求或回复模板话；例如问“你是什么模型”时应回答当前 Provider 和模型名；命令必须通过白名单校验，再用 `subprocess` 列表参数执行，避免 shell 注入；软暂停写 `logs/control/pause.json`，工作流在阶段边界保存 checkpoint 后暂停；`/api/progress` 读取 `logs/control/<任务ID>.checkpoint.json` 并返回 `latest_checkpoint`，页面检测到断点后启用“继续暂停任务”，点击后执行 `approve <任务ID>`，由已有执行器从下一阶段恢复；强制停止必须多重确认并由后端校验 `STOP`，只中断当前后台进程，不删除 API key；进度树从 `logs/plans`、`logs/workflows`、checkpoint 和实时进度读取状态，在黑底终端式页面里渲染节点和连线。
- 树形约定：终端树不是普通竖排步骤列表；主干应保持 `ContextPack -> OrchestratorAgent -> 人工审批 -> KnowledgeAgent -> ReviewerAgent -> ReflectorAgent/Done`，只有 `CoderAgent(A/B)` 从 `KnowledgeAgent` 右侧分支出去。当前节点和已走过路径用紫色，未到达节点和虚线为黑灰色。
- 怎么验证：`tests/test_webui.py` 覆盖命令白名单、帮助、provider 输出、进度快照、checkpoint 续跑入口、页面关键控件，以及 `KnowledgeAgent` 下挂 `CoderAgent` 的树布局回归；浏览器检查桌面/窄屏可视化进度树可见、控制台无错误。
- 常见坑：不要把任意 shell 命令透传到页面；服务只监听 `127.0.0.1`，不要改成 `0.0.0.0` 暴露到局域网；用户说“只改树”时，不要改按钮、顶部栏、输入框、终端外壳或提示文案。跑全量测试前把当前进程的 `AGENT_WORKBENCH_PROVIDER` 临时设为 `mock`，避免用户系统环境已配置 DeepSeek/OpenAI 时误走真实模型、卡住或消耗 API key。

## 10. 参考 Codex 做工具型 Web UI

- 做什么：Web UI 参考 Codex 的信息架构：左侧项目/命令列表，中间任务流，底部输入框，右侧环境信息面板，右上角用 3 个图标键控制布局区域。
- 为什么：本项目是开发工具，不是营销页或数据仪表盘；工具型界面要低噪音、高密度、少装饰，让用户专注任务和输出。
- 怎么判断参考图重点：先问这张图要学的是视觉样式、信息架构还是交互模式。比如 Codex 右上角按钮的重点不是“圆角图标长相”，而是“点一下显示/收起对应面板”的布局控制行为。
- 怎么做：只展示已实现功能；顶部图标键控制左侧导航、运行输出、右侧环境信息的展开/收起；不要把未实现的编辑器、账号、插件市场等功能做成假入口。
- 怎么验证：浏览器实测页面可打开、无 console 警告；点击 3 个布局键时，对应区域能收起，再点能展开；命令按钮仍能真实调用 `/api/command`。
- 常见坑：把参考截图理解成“做得更花”会导致 AI 味仪表盘；把图标按钮做成普通命令按钮也不对，图标按钮必须有明确的布局状态和 `aria-pressed`。

## 11. 草稿评审（ReviewerAgent）

- 做什么：CoderAgent 写完代码草稿后，ReviewerAgent 做规则版评审：草稿是否存在、`## AI 草稿` 正文是否太空（正文 < 100 字符）、是否包含敏感信息、是否越权（引用项目目录外的主知识库路径）。
- 为什么：AI 生成的草稿不能直接往下流，先检查再进入验证环节；不通过时最多打回 CoderAgent 重写一次，第二次仍不通过就停下交给人工处理。
- 怎么做：`_find_drafts` 按 `<任务ID>-coder-draft*.md` 定位草稿，并按 worker 只取最新版（如 `-revision1` 覆盖第一版）；复用 `review.scan_sensitive` 做敏感扫描；越权检查对比 `config.main_vault_path` 与 `project_root`，项目目录内路径允许，项目目录外的主知识库路径拦截；结论写入 `last_verdict` / `last_reasons` 实例属性，并体现在 `AgentResult.outputs`。
- 怎么验证：`tests/test_reviewer.py` 覆盖无草稿、空草稿、正常草稿、只审最新版、敏感信息、越权引用六类场景；`tests/test_workflow.py` 覆盖默认流水线打回重写与重写后仍失败停止。
- 常见坑：不能用整个草稿文件长度判断是否太空，因为文件头和安全边界会把空草稿撑长；必须只看 `## AI 草稿` 正文。项目本身位于主知识库内，越权检查不能见到主知识库前缀就拦截，否则正常的项目日志路径会误判。

## 12. 自适应编排（OrchestratorAgent + run-adaptive）

- 做什么：主控按任务复杂度从三档预设模板选执行方案，阶段化执行，阶段内可并行：SIMPLE=2 workers（Coder→Reviewer）、MEDIUM=3 workers（Knowledge→Coder→Reviewer）、COMPLEX=4 workers（Knowledge→双 Coder 并行→Reviewer）；`plans` 命令列出已保存方案，方便人工审批前查看。
- 为什么：半动态——不自由拆分（稳定、可预期），但 worker 数量随复杂度变化（灵活）；阶段间串行尊重 Knowledge→Coder 依赖，避免 Coder 没拿到检索结果就乱写。
- 怎么做：`estimate_complexity`（规则打分：任务长度 / 拆分信号词 / 技术词数量）→ `estimate_complexity_with_provider`（AI 可选，解析失败回退规则版）→ `build_plan`（模板）→ `create_adaptive_plan`（写 `logs/plans/<任务ID>.json` 等待审批）→ `plans` 查看方案状态 → `approve` 后 `execute_adaptive_plan`（阶段内 `ThreadPoolExecutor` 并行）；Web UI 直接输入普通内容默认走 `/api/discuss`，由 OrchestratorAgent 前置讨论员先追问澄清，点击“生成主控方案”才把讨论内容送入 `run-adaptive`，点击“开始协同工作”才对最近方案执行 `approve`；执行阶段支持软暂停，阶段完成后保存 `logs/control/<任务ID>.checkpoint.json`，恢复时从下一阶段继续；CoderAgent 用 `worker_label` 区分并行 worker 写独立草稿；ReviewerAgent 评审最新版草稿，不通过时让上一阶段 Coder worker 最多重写一次。
- 怎么验证：`tests/test_orchestrator.py` 覆盖复杂度分级、模板 worker 数量、主控输出 plan；`tests/test_adaptive_workflow.py` 覆盖审批闸门（计划阶段不执行、批准后执行）、方案列表状态、三档端到端（SIMPLE 无检索、MEDIUM 检索先行、三档均 Coder 后接 ReviewerAgent）和复杂任务双 Coder 打回重写。
- 常见坑：并行 worker 写文件必须用独立文件名（`worker_label` 后缀），否则互相覆盖；重写草稿也必须带 `-revisionN` 后缀，避免覆盖第一版证据；mock Provider 输出无法解析复杂度时自动回退规则版，别把回退当失败；阶段内并行共享 `results` 列表只读，不要在 worker 里修改共享状态；阶段间必须串行，否则 Coder 可能先于 Knowledge 执行；CLI 真实路径必须走"计划→plans 查看→approve→执行"两阶段，不要让列表命令隐式执行方案；程序化 `run_adaptive_workflow` 只用于测试，避免绕过人工审批。

## 13. PyInstaller 打包 Web 终端为 Windows 软件

- 做什么：把 Web 终端（`webui.py`）打包成双击即用的 exe；由于 `run_cli` 用 subprocess 调 `python -m code_agent_collab.cli`，打包后 `sys.executable` 是 exe 本身无法 `-m` 启动，所以拆成两个 exe：`MultiAgentWorkbench.exe`（界面，`--windowed`）+ `AgentWorkbench-CLI.exe`（后台 CLI，`--console`），界面在 frozen 模式下调用同目录的 CLI exe。
- 为什么：用户要"打包成一个软件"，双击就能打开网页界面；onefile 单文件模式便于分发。
- 怎么做：`webui.py` 用 `getattr(sys, "frozen", False)` 区分打包/开发模式（frozen 时 `PROJECT_ROOT` 取 `Path(sys.executable).parent`，因为单文件模式没有 `__file__`）；`run_cli` frozen 分支拼 `Path(sys.executable).parent / "AgentWorkbench-CLI.exe"`；`main()` 启动后 `threading.Timer` 延迟 1 秒 `webbrowser.open` 自动打开浏览器（`--no-browser` 可关）；**入口不能用包内模块**（`cli.py`/`webui.py` 在包内用相对导入，PyInstaller 当顶层脚本执行会报 `ImportError: attempted relative import`），必须建 `scripts/launcher_cli.py`、`scripts/launcher_webui.py` 顶层入口再 `from code_agent_collab.xxx import main`；打包脚本 `scripts/build-exe.ps1` 用 `--paths src` 指定包路径（`--distpath dist --workpath build --specpath build`，spec 不污染仓库根）；下载包用 `scripts/package-windows.ps1` 生成 `dist/AgentWorkbench-v版本号-windows.zip`，同时放入双 exe、Provider 配置脚本和启动说明。
- 怎么验证：`python -m unittest discover -s tests` 通过；启动 `dist/MultiAgentWorkbench.exe` 后访问 `http://127.0.0.1:8080` 页面可打开、命令可执行。
- 常见坑：改 Web UI 源码后必须重新运行 `pwsh -File scripts/package-windows.ps1` 并验证 `dist/MultiAgentWorkbench.exe`，否则用户双击旧 exe/旧 zip 时看不到新交互；CLI exe 必须 `--console` 模式，`--windowed` 会没有 stdout 导致界面拿不到输出；frozen 模式下没有 `__file__`，路径必须基于 `sys.executable`；入口必须是顶层脚本（包内模块的相对导入在 PyInstaller 下会失败）；onefile 首次启动慢（要解压到临时目录），属正常现象；exe 未签名，Windows SmartScreen 可能提示，需"仍要运行"；打包脚本必须保持纯 ASCII（PowerShell 5.1 解析无 BOM UTF-8 中文会乱码报错）。

## 14. apply-draft 草稿应用与安全边界

- 做什么：把规范化的 Coder 草稿解析成"文件 → 完整新内容"，dry-run 预览 diff，`--apply` 应用 → 自动跑测试 → 测试通过自动本地 commit；测试失败自动回滚。
- 为什么：让 AI 产出从"建议文档"变成"能安全落到正式项目"的能力；预览、确认、测试、回滚四道闸门保证不把项目改坏。
- 怎么做：CoderAgent 提示词强制五小节格式（修改文件清单/修改原因/建议代码/测试方法/风险），建议代码用 `### <路径>` 块给出完整新内容；`apply.py` 的 `parse_draft` 按小节解析 → `validate_changes` 白名单校验（只允许 `src/`、`tests/` 下文本文件）→ `generate_diffs` 用 difflib 生成 unified diff → `--apply` 时先查 `git_is_clean`（脏工作区拒绝）→ 写文件并备份 → `run_tests` → 失败 `_rollback_changes` 还原、成功 `git_commit`。
- 怎么验证：`tests/test_apply.py` 覆盖解析、白名单拒绝、dry-run 不写文件、失败回滚、通过后提交、脏工作区拒绝；CLI 冒烟跑通 dry-run → apply → commit 全链路。
- 常见坑：测试临时仓库必须写 `.gitignore`（否则草稿文件会污染 git status，前置检查永远失败）；Windows 上 Python 原生 `Path.write_text` 默认把 `\n` 转 CRLF，与项目 `file_utils.write_text`（固定 LF）混用会导致"回滚后 git 仍显示已修改"，测试初始化统一用项目 `write_text` 或显式 `newline="\n"`；mock Provider 生成的草稿没有固定小节无法解析，apply 只对真实 AI 草稿有意义；应用前必须保证 git 干净，否则无法干净回滚。

## 常用命令

```powershell
$env:PYTHONPATH="src"
python -m code_agent_collab.cli start "任务目标"
python -m code_agent_collab.cli run "任务目标"
python -m unittest discover -s tests
```

## 15. 协作前置体检（不动代码）

- 做什么：每次接手这个项目，先确认真实源码根、Git 跟踪关系、本地与远端差异、当前 Provider 模式，再决定是否进入代码修改。
- 为什么：这个项目常从 `Documents\ChatGPT\agent协作` 任务目录发起，但真实源码根在 `C:\Users\lwz12\Desktop\AI工作台知识库\01-项目\project 多Agent代码协作助手`；如果目录认错，会把服务状态、Git 状态和版本状态判断错。
- 怎么做：
  1. 进入真实项目根：`cd "C:\Users\lwz12\Desktop\AI工作台知识库\01-项目\project 多Agent代码协作助手"`。
  2. 看分支关系：`git status --short --branch`。
  3. 若当前分支没有 upstream，执行：`git branch --set-upstream-to=origin/main main`。
  4. 看本地与远端提交：`git rev-parse HEAD` 和 `git ls-remote origin refs/heads/main`。
  5. 测试或评估时先设 mock：`$env:AGENT_WORKBENCH_PROVIDER="mock"`，避免误走真实 API。
- 怎么验证：`git status --short --branch` 能显示 `main...origin/main [ahead N]` 或 `[behind N]`；`python -m unittest discover -s tests` 在 mock 模式下通过。
- 常见坑：本地 ahead 不是坏事，只代表 GitHub 还没同步；不要把“GitHub 旧”误判成“本地代码没保存”。发布或协作前必须先说明哪些提交还只在本机，不能直接按 README 里的版本号判断。

## 16. 当前架构限制清单

- 做什么：维护时把当前项目按“本地优先 Agent 协作原型”看待，不要误当成成熟多人协作平台。
- 为什么：项目已经有主控方案、并行 Coder、Reviewer、Web 进度树和 apply-draft，但仍有几个原型级限制。
- 怎么做：改需求或规划版本前先核对这些限制：
  - 进度已经有后端任务级快照 `logs/progress/<任务ID>.json`，但 Web UI 仍默认读兼容用的 `current.json` / latest 视角；多任务同时跑时不会丢失各自快照，但页面还没有多任务选择器。
  - 阶段内并行 worker 共享同一份上下文和结果列表，适合只读参考；并行 Coder 已有 `owned_paths` 基础职责边界和共享黑板记录，但还没有真正的跨进程文件锁或 Integrator 合并角色。
  - Web UI 进度树还不能点开单个 Agent 查看职责、认领路径、输出、失败、阻塞和下一步；后端 blackboard 数据已经准备好，下一步可做最小详情面板。
  - ReviewerAgent 主要是规则版检查，不等于真实代码审查通过。
  - 真实 Provider 调用只有基础超时和错误处理，缺少重试、限流、费用预算和结构化输出校验。
  - Web UI 通过子进程运行 CLI，长任务或真实模型慢响应时要关注超时、后台进程和进度状态是否一致。
  - `apply-draft` 是实验能力，依赖固定 Markdown 格式和整文件内容，复杂修改仍要人工 review diff。
- 怎么验证：规划新版本时，把上述限制写进变更记录或版本说明；涉及 Web UI/Provider/编排的改动，除了单测，还要验证实际页面或打包后的用户入口。
- 常见坑：mock 测试通过只能证明基础流程没断，不能证明真实模型、长任务、多任务并发、发布包都稳定。

## 17. WorkerRun 子 Agent 状态账本

- 做什么：自适应编排在阶段内执行 worker 时，逐个记录 `running/success/failed/skipped` 状态到账本 `logs/runs/<任务ID>/workers.json`。
- 为什么：原来的 checkpoint 主要记录“下一阶段从哪里继续”，无法精确表达同一阶段里哪个子 Agent 成功、哪个失败；并行 Coder 中 A 成功 B 失败时，重跑不应该让 A 白干第二遍。
- 怎么做：`worker_runs.py` 提供 `start_worker_run`、`finish_worker_success`、`finish_worker_failed`、`should_skip_worker`、`mark_worker_skipped`；`orchestration.py` 用 `_run_single_worker` 包装真实 Agent 调用，先写 running，成功写 success，异常写 failed。输入 hash 基于任务目标、上下文包、角色、label；Reviewer 打回重写时额外把 revision 和反馈内容纳入 hash，避免误复用上一轮草稿。
- 怎么恢复：某一阶段部分失败时，主控保留已成功 worker 的 `AgentResult`，把 checkpoint 的 `next_stage_index` 保持为当前阶段；下次 `approve <任务ID>` 重新进入同一阶段，账本中同输入已成功的 worker 会标记为 `skipped` 并复用结果，只补跑失败或缺失 worker。
- 怎么验证：`tests/test_adaptive_workflow.py::WorkerRunLedgerTests` 模拟两个并行 Coder：模块A 成功、模块B 失败；第二次运行时模块A 的失败替身不会被调用，模块B 正常补跑，账本状态从 `success/failed` 变为 `skipped/success`。
- 常见坑：并行 worker 会同时写同一个账本文件，Windows 下只靠 `.tmp` + `os.replace` 仍可能遇到文件占用；必须用进程内锁串行化账本写入，并给临时文件使用线程独立名称。`skipped` 也要被视为可复用状态，否则第一次跳过后下一次会失去缓存效果。当前账本是本地单进程内安全，不等于多进程/多机器分布式锁。

## 18. 进度快照任务级简单隔离

- 做什么：`publish_progress` 继续写 `logs/progress/current.json` 兼容现有 Web UI，同时额外写 `logs/progress/<任务ID>.json` 和 `logs/progress/latest.json`。
- 为什么：当前不急着做多任务并行 UI，但后端先不要只有一个“当前任务屏幕”；即使以后多个任务交替写进度，也能保留每个任务自己的最后快照。
- 怎么做：`progress.py` 新增 `task_progress_path`、`latest_progress_path`，`read_progress(project_root, task_id=...)` 可以读取指定任务；自定义 `AGENT_WORKBENCH_PROGRESS_FILE` 时仍保持旧的单文件行为，方便测试或特殊运行环境隔离。
- 怎么验证：`tests/test_progress.py` 覆盖连续发布 task-a/task-b 后，`current.json` 指向最新任务，`task-a.json` 和 `task-b.json` 各自存在且可按 task_id 读取；同时覆盖自定义进度文件时不额外生成任务级文件。
- 常见坑：这只是后端简单隔离，不等于 Web UI 已支持多任务列表；当前页面仍默认展示最新/当前任务。Windows 下写 JSON 快照同样可能遇到目标文件短暂占用，临时文件名要带 pid/thread id，并对 `PermissionError` 做短重试。

## 19. WorkerSpec 职责路径边界

- 做什么：`WorkerSpec` 增加 `owned_paths`，让每个 worker 除了角色和标签，还能声明自己负责哪些路径；复杂任务默认拆成 `CoderAgent(实现)` 负责 `src/`，`CoderAgent(测试)` 负责 `tests/`。
- 为什么：只写“模块A/模块B”太模糊，两个 Coder 容易重复做同一段业务；先加路径级任务契约，后续 Agent 数量变多时可以继续扩成更多角色和更细目录。
- 怎么做：`orchestrator.py` 的复杂模板写入非重叠 `owned_paths`；`orchestration.py` 保存结构化计划 JSON，并兼容旧数组格式；并行执行前用 `_ensure_stage_contracts_do_not_overlap` 检查同阶段 worker 的路径是否相同或互相包含，冲突时抛出 `WorkerContractConflict`；`CoderAgent` 的提示词和草稿头部写明负责路径；`worker_runs.py` 的输入 hash 纳入 `owned_paths`。
- 怎么验证：`tests/test_orchestrator.py` 覆盖复杂模板的实现/测试分工；`tests/test_adaptive_workflow.py` 覆盖路径冲突拦截和新旧计划 JSON 兼容；`tests/test_webui.py` 覆盖 Web UI 后端读取带 `owned_paths` 的计划格式。
- 常见坑：这是“软边界 + 执行前冲突检测”，不是最终的文件锁。以后要让更多 Agent 长时间并行，仍需要共享黑板、路径租约、Integrator 合并阶段和实际 diff 越界检查。

## 20. 共享黑板 Blackboard

- 做什么：每个自适应任务写一个共享黑板 `logs/blackboards/<任务ID>.json`，记录每个 Agent 的职责、负责路径、状态、输出、阻塞和备注。
- 为什么：WorkerRun 更偏执行账本和重跑依据；黑板更偏“给人和后续 Agent 看”的协作状态。以后 Agent 数量变多时，不能只靠初始 prompt 各干各的。
- 怎么做：`blackboard.py` 提供 `mark_agent_planned/running/success/failed/skipped` 和 `blackboard_snapshot`；`create_adaptive_plan` 生成计划后写 planned；`_run_single_worker` 在执行前写 running，成功写 success，异常写 failed，复用缓存写 skipped；`webui.build_progress_snapshot` 把 blackboard 放进 `/api/progress` 返回。
- 怎么验证：`tests/test_blackboard.py` 覆盖生命周期、并发写入和失败 blocker；`tests/test_adaptive_workflow.py` 覆盖真实 workflow 写黑板；`tests/test_webui.py` 覆盖进度快照暴露 blackboard。
- 常见坑：黑板是受控状态记录，不是自由聊天；worker 只能写自己的记录。Windows 下并发写 JSON 要用进程内锁、独立临时文件和 `os.replace` 重试。当前 blackboard 还没有合并 WorkerRun 的 attempt 次数，做 UI 详情面板时再整合。

## 21. 后续 UI：Agent 详情面板

- 做什么：以后在 Web 终端进度树里支持点击 Agent 节点，打开详情面板查看该 Agent 的职责范围、认领路径、运行状态、输出路径、失败原因、阻塞信息和下一步。
- 为什么：只看“CoderAgent 正在运行/已完成”不够；用户需要能判断每个 Agent 到底在做什么、有没有重复做事、哪里卡住了。
- 怎么做：不要重做整个 UI。直接使用 `/api/progress` 返回的 `blackboard`，再补 WorkerRun attempt/error 细节，最后在前端给进度树节点加点击事件和详情面板。
- 怎么验证：UI 完成后用浏览器检查点击每个 Agent 节点都能看到对应详情，且窄屏不遮挡主流程。
- 常见坑：详情面板的数据应来自结构化运行态，不要从日志 Markdown 里临时解析；Agent 间通信要受控，只允许写自己的黑板记录或按规则声明 claim，不能变成自由聊天抢活。
